# 概览页比例视觉验收

## 参考输入

- `/Users/a1234/Desktop/截屏2026-09-09 22.02.17.png`：概览页顶部同步状态与操作按钮。
- `/Users/a1234/Desktop/截屏2026-09-09 22.02.23.png`：概览页四个状态指标卡及下方内容区。

## 本次调整

- 指标卡增加 `metric-value-line` 值组，统一数值与说明的间距、基线和换行行为。
- 刷新与通知按钮使用固定 32px 尺寸、无内置按钮留白并进行网格居中。
- 顶部头像统一为 32px，与操作按钮保持同一控件比例。

## 渲染核对

实现页面：`http://localhost:5173/overview`

- `/tmp/overview-after-1280.png`：桌面截图，指标卡值组清晰分隔，顶部控件尺寸一致。
- `/tmp/overview-after-390.png`：窄屏截图，侧边栏与内容区正常堆叠，操作组未溢出。
- 1280px：指标卡 227x108px；刷新按钮和头像均为 32x32px；无横向溢出。
- 768px：指标卡两列，单卡 246x108px；无横向溢出。
- 390px：指标卡单列，单卡 362x108px；无横向溢出。

## 验证结果

- `npm run test:run`：19 个测试文件、77 个测试全部通过。
- `npm run build`：构建通过；仅有既有 chunk size 提示。
- `npm run e2e`：22 个通过、3 个按条件跳过、0 个失败。
- `git diff --check`：通过。

final result: passed
