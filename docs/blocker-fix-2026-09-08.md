# 项目阻塞修复记录（2026-09-08）

## 范围与源码身份

- 仓库：`/Users/a1234/Documents/class schedule`，分支 `main`。
- 基线 HEAD：`6a0b1361ea7f4993d113e30c8592660715ca872e`。
- 验证对象是当前未提交工作区，包含已有邮箱注册改动；不能把结果归到基线提交。
- 本次修复：约束匹配索引、前端注册开关配置链路及测试、本地运行环境更新。
- 保留用户已有改动；未提交、推送或部署生产。

## 修复内容

1. 教师、班级、教室与活动组约束先按关联键建立匹配索引，再执行原有约束判断，减少无关课次配对。未降低硬约束或延长基准的 10 秒预算。
2. 前端开发和构建读取根目录 `.env` 的 `APP_AUTH_REGISTRATION_ENABLED`，解决按部署说明启用后端注册却看不到前端入口的问题。显式进程变量 `VITE_AUTH_REGISTRATION_ENABLED` 仍可覆盖；其他根目录环境配置不会通过此映射注入前端。
3. 适配 Vitest 配置合并，补充注册入口启用/禁用测试，关闭注册时隐藏无入口的页脚提示，并更新 README。
4. 修复新镜像实际启动后健康接口返回 503：注册默认关闭时仍启用了 SMTP 健康检查。现在邮件健康检查跟随注册开关；注册开启时继续检测邮件服务，避免掩盖真实故障。新增启用/禁用两种状态回归。

## 已完成验证

- 后端最终 `mvn -q -f backend/pom.xml verify`：通过，163 项发现，其中基准默认跳过 1 项，0 失败、0 错误。
- 约束测试与启用基准的定向运行：通过。
- 前端：11 个文件、57 项测试通过；生产构建通过。
- 实际 Vite 配置加载：后端开关 true/false 均正确映射到前端。
- 240 课次基准连续三次通过：均为 10 秒预算、全部分配、`0hard/0medium`；软分依次为 -96、-84、-112，实测耗时 10011、10015、10010 ms。
- 修复前本轮基准也曾通过，而上一轮失败；这是限时求解波动，不能声称原实现必然失败或本次能保证任意数据可行。
- 升级前本地数据库备份：`.phase6-evidence/blocker-fix-20260908/backups/class_schedule_20260908-220332.dump`。
- 备份 SHA-256：`86c806e1212a977d3a9d3b0ffab1ede492ddff8648186b35b448170e11fd815d`。

## 运行验证

- 最终 API/Worker 镜像已重建并替换本地旧容器，PostgreSQL 数据卷保留，Flyway 实查为 V33。
- `/actuator/health` 返回 HTTP 200、`{"status":"UP"}`，浏览器回归期间复查仍正常。
- API 镜像：`sha256:e6bf657d5e85ebd3d5f19036d21a6285e0e792e91533cbc003a3579edb699bdd`。
- Worker 镜像：`sha256:9e68eb88aefe826507e9bc07cb783b2cbbbac94523f2b62f37c2d85f2aee4491`。
- 两个运行容器 `/app/app.jar` SHA-256 一致：`9725b7fc0c608eab7f03ae3ba56445156e7c75d437a69671dc36354e8405633f`。
- 最终镜像下 Playwright：21 条发现，19 条通过、2 条跳过、0 失败，1.7 分钟。覆盖 AI 诊断、导入预检/确认/幂等、真实 Excel/PDF/校验报告下载、版本复制/锁定、视图切换与提交求解到候选结果。
- 两条跳过均属于邮箱注册：当前注册关闭，且未提供真实邮箱验证码；不能记为注册端到端通过。
- 浏览器回归对本地测试数据执行了导入、创建草稿和求解；升级前备份保留，未接触生产数据。
- 1280×800 桌面与 390×844 手机登录页截图检查通过，无页面脚本错误、无横向溢出，注册关闭时不存在空注册提示。
- 当前页面：`http://localhost:5173`。
- `git diff --check`：通过。
- 本轮日志与截图保存在本地忽略目录 `.phase6-evidence/blocker-fix-20260908/`，不包含于源码提交。

## 保留的交付条件

- 当前邮箱注册默认关闭；尚无真实 SMTP 邮箱收发验收证据。
- 未提交的已有改动仍需按交付范围整理，未将全部工作区内容夹带提交。
- 真实学校数据 UAT、完整用户管理/RBAC 产品范围、复杂规则覆盖、告警/Outbox、数据库运行账号最小权限、许可证确认、正式 RPO/RTO 与生产回归仍不应标记完成。
- 本次合成基准与本地浏览器结果不代表生产交付验收。
