# 回归清单 — 2026-09-09

- **当前基线**：HEAD `b730b2f` + 当前工作区变更；本轮复测覆盖 AI 模型配置入口、Anthropic/MiniMax 兼容协议和既有排课主链。
- **历史基线**：旧版报告中的 HEAD `6a0b136` 记录保留在本文后续历史条目中。
- **变更类型**：AI 模型配置与权限边界 + Anthropic 兼容协议 + 既有复盘竞态修复回归。
- **关联 Bug**：BUG-20260909-001（与 Bug 条目的回归建议双向关联）。

## 本轮完整回归复测（当前工作区）

| 验证面 | 结果 | 结论 |
|---|---:|---|
| 后端全量 `mvn -f backend/pom.xml test` | 173 tests，172 passed，1 intentional skip，0 failure/error | 通过 |
| 前端全量 Vitest | 19 files，77 passed，0 failure | 通过 |
| 前端生产构建 | 通过；仅有既有 chunk size warning | 通过 |
| 重建后 Docker 运行态 | API/worker/PostgreSQL 运行；`/actuator/health` 返回 `200 UP` | 通过 |
| 完整 Playwright E2E | 25 tests，22 passed，0 failed，3 skipped | 有条件通过 |
| 变更 Java 文件 Spotless + `git diff --check` | 通过 | 通过 |

本轮 3 项跳过为邮箱验证码注册 2 项（环境未配置 SMTP/验证码服务）和版本 fork 1 项（环境没有满足条件的可复制发布版本），不计为产品失败。

### AI 配置专项

| 用例 | 覆盖点 | 证据 | 结果 |
|---|---|---|---|
| `TC-AI-ADMIN-01` | `USER_ADMIN` 入口、API Key 不回显、保存/保留/清除、必填校验和非管理员边界 | `AiSettingsView.spec.ts`、AI Controller/Service 集成测试 | 通过 |
| `TC-AI-ANTHROPIC-01` | Anthropic `/v1/messages` 请求头、请求体、响应解析；自定义 base URL 可使用 MiniMax Anthropic 兼容地址 | `AiChatServiceTest`、前端 AI 设置测试 | 通过 |
| `TC-AI-MIGRATION-01` | V39 AI 配置表、V40 协议字段迁移；Flyway 升级校验 | 后端 Testcontainers 全量测试，40 migrations validated | 通过 |
| `TC-AI-RUNTIME-01` | 重建镜像启动、API 健康端点和 Planner AI 主流程 | Docker Compose、Playwright `ai.spec.ts` | 通过 |

正向 `USER_ADMIN` 浏览器登录设置流程未执行：当前本地数据库没有可用的 `USER_ADMIN` 测试账号；权限代码、接口集成和前端表单行为已覆盖。真实 MiniMax 请求也未发送，避免在回归中传输真实凭据；当前协议验证使用本地 mock。

## 必须回归（P0）

| 用例/批次 | 依据 | 结果 |
|---|---|---|
| `TC-R-01` 复盘旧响应不能覆盖当前状态 | Bug 修复直接命中 `RetrospectiveView.vue` 的 `load()`；单测锚点 + E2E 复验 | 通过 |
| `TC-P1-04` 复盘保存刷新读回 | Bug 条目直接修复验证，覆盖真实用户输入和接口读回 | 通过 |
| P0 浏览器核心主链 19 条 | 导入、工作台、AI、版本、发布和导出核心路径 | 19/19 通过 |
| 完整浏览器批次 25 条 | 含邮箱注册和版本 fork 前置条件检查 | 22/25 通过，3 跳过，0 失败 |
| 前端 77 条单测 | 组件/工具/状态共享逻辑回归，含 AI 设置表单 | 77/77 通过 |
| 后端 `mvn -f backend/pom.xml test` | Controller、权限、迁移、求解、导入、导出和 AI 服务全回归 | 173 tests，172 passed，1 intentional skip，0 failure/error |
| 配置敏感数据扫描 | 发布示例配置不得包含真实 provider key；命中项需脱敏并保留本地轮换待办 | 通过（示例已脱敏） |

## 建议回归（P1）

| 用例/批次 | 依据 | 结果 |
|---|---|---|
| `TC-P1-01` P1 路由与当前学期数据 | 同一学期 store 和多个 P1 页面消费方 | 通过 |
| `TC-P1-02` 问题创建到解决 | P1 Controller 和状态机消费方 | 通过 |
| `TC-P1-03` 学期复用预览 | P1 term copy 接口与页面数据映射 | 通过 |
| `TC-P1-05~06` 通知/规则模板 | 既有 Vitest + Controller 集成覆盖；浏览器路由 smoke 通过 | 通过 |

## 可选回归（P2）

- 移动端布局、像素级视觉基线、性能/并发、i18n、真实 MiniMax 外部调用、真实学校 UAT、生产部署：本轮策略排除或缺少外部前置，不能从本报告推断通过。

## 明确无影响

- 后端复盘接口 SQL、Flyway V37、P1 其他 Controller 未因本次竞态修复改动；由本次后端全套验证覆盖。
