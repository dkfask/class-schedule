# 复盘页面竞态复现记录 — 2026-09-09

## 复现结果

- **状态**：Verified，已复现并已修复。
- **环境**：`http://localhost:5173`、Chrome channel、Planner、当前学期 `2026-FALL`。
- **步骤**：登录 → 进入 `/retrospective` → 等待“产品与流程”出现 → 在“学校反馈”填写 `RACE-CHECK` → 连续观察字段值。

## 网络与页面时间序列（E3）

```text
1183ms  GET /api/terms
1397ms  GET /api/terms
1416ms  GET /api/retrospectives?termCode=2026-FALL
1417ms  GET /api/retrospectives?termCode=2026-FALL
1510ms  复盘读取返回，页面字段为既有值 DEBUG-RETRO-178893
1548ms  用户填写 RACE-CHECK，字段即时显示 RACE-CHECK
1564ms  第二个复盘读取返回
1650ms  字段恢复为 DEBUG-RETRO-178893
2156ms  字段仍为 DEBUG-RETRO-178893
```

同轮抓包中 PATCH 复盘接口在字段稳定时能收到 `schoolFeedback` 并返回 200；数据库读取也能读到已保存值。因此根因不在接口契约或数据库写入。

## 修复后复验

- `RetrospectiveView.spec.ts`：通过，首次当前学期复盘读取为 1 次。
- `product-loop.spec.ts` 复盘闭环：通过，填写 → 保存 → 刷新 → 读回 → 恢复原值。
- 修复点：`RetrospectiveView.vue` 为异步加载增加序号和学期快照，旧响应不再回写 `data`、`form` 或结束当前请求的加载状态。
