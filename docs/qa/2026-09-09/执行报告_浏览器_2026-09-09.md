# 浏览器执行报告 — 2026-09-09

## 本轮重建后完整复测

| 批次 | 用例数 | 通过 | 失败 | 阻塞/跳过 |
|---|---:|---:|---:|---:|
| 当前工作区完整 E2E | 25 | 22 | 0 | 3 跳过 |

- 执行入口：`npm run e2e --workspace frontend`。
- API 使用当前工作区重建后的 Docker 镜像；健康检查返回 `200 UP`。
- 通过范围：AI 2、导入 5、P1 闭环 4、已发布与导出 4、版本 4、工作台 3，共 22 条。
- 跳过：邮箱验证码注册 2 条，因环境未配置 SMTP/验证码服务；版本 fork 1 条，因环境没有满足条件的可复制发布版本。
- 本轮没有浏览器或产品失败；系统管理员正向设置页因本地没有可用 `USER_ADMIN` 账号未执行，后端权限与前端表单测试已覆盖。

## 历史首轮执行统计

### 结果明细（历史记录）

| 批次 | 用例数 | 通过 | 失败 | 阻塞/未执行 |
|---|---:|---:|---:|---:|
| 全量 E2E 首轮 | 25 | 22 | 1 | 2 跳过 |
| 首轮失败复跑 | 1 | 1 | 0 | 0 |
| 最终有效结果 | 25 | 23 | 0 | 2 跳过 |

## 结果

- P0 核心：导入 5、工作台 3、AI 2、版本 5、已发布与导出 4，共 19 条，最终全部通过。
- P1 产品闭环：路由/通知/审计/问题/规则模板/复盘/学期复用 4 条，最终全部通过。
- 跳过：邮箱验证码注册 2 条，原因是测试环境未配置 SMTP/验证码前置；不是产品失败。
- 首轮 AI 失败发生在 `browser.newContext` 浏览器启动超时，未进入页面和接口；空闲后单独重跑 34.1s 通过。

## 执行资产

- 既有测试：`frontend/e2e/import.spec.ts`、`workspace.spec.ts`、`ai.spec.ts`、`versions.spec.ts`、`published.spec.ts`。
- 新增 Page Object：`frontend/e2e/pages/PlanningProductPage.ts`。
- 新增闭环用例：`frontend/e2e/product-loop.spec.ts`。
- 失败分流：`失败分流_2026-09-09.md`。
- 敏感数据审计：`.env.prod.example` 已替换为占位符；本地 `.env` 未改动，需凭据持有者轮换其 AI provider 凭据。
