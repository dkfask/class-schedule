# 需求模型（requirement-analysis）

- 建模时间：2026-09-06
- 测试目标 revision：本地分支 `feat/optional-master-data-sheets` @ `6d8eac3`（tree `f4fdadd`，与 `origin/main` @ `08d585a` 源码内容一致，差异仅为 merge commit）
- 修订说明：会话期间 HEAD 曾从 `5657e4a` 前移至 `6d8eac3`；进入执行阶段前需重新核对 HEAD 未再变动

## requirement_model

```yaml
requirement_model:
  goal: >
    对当前 revision 做全项目健康验证：常规测试（后端 Maven + 前端 Vitest）保持全绿，
    并补齐昨日报告（docs/local-test-report-2026-09-05.md）明确缺席的真实浏览器端到端验收。
    成功标准：常规测试 0 失败 0 错误；浏览器端核心流程可走通且无阻塞级缺陷；
    昨日风险项 #1（主数据启用恢复流程）与 #3（中文 PDF 视觉验收）获得明确结论。
  scope:
    included:
      - 常规测试复核：后端 `mvn -f backend/pom.xml verify`（含 Testcontainers 集成测试）、前端 `npm run test:run`
      - 浏览器 E2E（桌面，先例视口 1280×720，Planner 角色）：
        登录/会话、主数据管理（重点：新增"启用/停用/恢复"流程）、教学计划（重点：切换学期后节次选项更新）、
        规则事实页、导入流程（模板下载、可选 Sheet 缺省导入、datasets/ 外部课表数据集转换预览）、
        排课工作台（求解状态、课表网格、无教室占位显示）、版本管理（列表/差异/锁定/命令历史）、
        已发布课表与导出（重点：中文 PDF 视觉验收、空课表 PDF 一页行为）
      - 重点风险区（本分支最新变更，昨日报告未在真实浏览器验证）：
        35bc740 主数据与 PDF 导出流程收尾、6d8eac3 启用接口接线与空 PDF 处理、
        be1af2e 外部课表转换器与格式后端、5657e4a/b96af76 无教室占位与硬可行候选
    excluded:
      - 性能/中型数据基准（README"尚未完成"显式排除；昨日合成基准已单测 0hard，非本轮回归目标）
      - 移动视口 E2E（昨日报告未验证项 #5，显式不在本轮）
      - 生产部署、备份恢复、镜像运行、全新机器依赖安装
      - 未实现功能：复杂合班拆分、分层走班、跨午休可配置规则、完整用户管理/RBAC 策略
  roles:
    - role: Planner（bootstrap 账号）
      can: 全部 PLANNER 级 API：master-data、rule-facts、schedule-rules、imports、solve-jobs、
           publish/adjustments/lock/archive/fork
      cannot: 无 PLANNER 角色的用户访问上述 API 返回 403；未认证返回 401
      evidence: { level: E2, source: "backend/src/main/java/com/classschedule/security/SecurityConfig.java:43-70" }
  inputs:
    - MASTER_DATA v1 Excel 模板（中文列名；教室、资源可用性、特征、活动组为可选 Sheet，可省略或留空）
      evidence: { level: E1, source: "README.md 导入导出节" }
    - 外部课表数据集样例：datasets/{k12-sample, small-feasible, small-infeasible, over-constrained, itc2007-comp01-medium}
      evidence: { level: E2, source: "datasets/ 目录与 docs/external-dataset-converter.md" }
    - 登录凭据：bootstrap Planner（APP_AUTH_BOOTSTRAP_USERNAME/PASSWORD，本地默认 planner / change-this-in-local-env）
      evidence: { level: E1, source: ".env.example" }
  outputs:
    - 求解结果 schedule_assignment → 工作台课表网格（班级/教师/教室三维视图）
    - 版本对象（schedule_version）→ /versions 页面与发布门禁
    - 导出物：课表 xlsx、课表/校验报告 PDF → 浏览器下载
  states:
    - "schedule_version: DRAFT --求解完成--> CANDIDATE --publish--> PUBLISHED；DRAFT/CANDIDATE --archive--> ARCHIVED；发布门禁拒绝则停留 CANDIDATE"
      evidence: { level: E1, source: "README.md 版本与发布节；docs/visual-test-report.md 已见 PUBLISHED/CANDIDATE/DRAFT/ARCHIVED/CANCELLED" }
    - "solve_job: 提交后排队→运行→完成/失败/取消；deadline 15 分钟；过期恢复"
      evidence: { level: E1, source: "README.md 异步求解 Job 节" }
    - "主数据条目: active ↔ 停用；导入可选 Sheet 缺省不删除已有配置，只有明确停用/解绑行才修改"
      evidence: { level: E1, source: "README.md 导入导出节；6d8eac3 启用接口接线" }
  rules:
    - rule: 发布前按目标学期教学需求对账，拒绝漏排、重复、额外 occurrence、需求身份/周课时/时长/固定节次篡改
      evidence: { level: E1, source: "README.md 发布对账条目" }
    - rule: 输入快照或规则快照变化后发布返回 INPUT_SNAPSHOT_STALE
      evidence: { level: E1, source: "README.md 版本输入快照哈希条目" }
    - rule: 已发布/归档版本数据库级不可变（assignment、版本行、命令历史直接写入被触发器拒绝）
      evidence: { level: E1, source: "README.md；V27 迁移" }
    - rule: 导入模板校验问题码（UNKNOWN_SHEET/DUPLICATE_SHEET/INVALID_SHEET_ORDER/MISSING_SHEET/EXTRA_COLUMN 等），失败事务回滚、重复确认保护
      evidence: { level: E2, source: "backend/.../WorkbookImportService.java:250-330" }
    - rule: MASTER_DATA Sheet 顺序必须遵循模板顺序；无教室的 occurrence 在工作台保持 roomless 占位
      evidence: { level: E2, source: "WorkbookImportService.java:266-313；commit 5657e4a" }
    - rule: 求解候选必须硬可行（真实课表候选 hard-feasible）
      evidence: { level: E2, source: "commit b96af76/93a4815" }
  exceptions:
    - 401 → 前端统一派发 auth:expired 事件（E2，frontend/src/api/http.ts:49-51）
    - 导入预览问题列表带中文诊断信息；确认失败整事务回滚（E1 README）
    - 冲突类修改返回统一冲突响应（VersionMutationException code + revision）（E2，VersionMutationException.java）
    - 空课表 PDF 返回一页（昨日 E3 已验证；本轮浏览器侧复核）
  dependencies:
    - PostgreSQL 16（docker compose，POSTGRES_* 变量必需；当前无 .env，需从 .env.example 注入默认值）
    - Flyway V1–V32 迁移
    - Timefold Solver（worker profile，:8081）
    - 前端 Vite dev server（:5173，代理 /api 到 :8080）
    - 浏览器宿主：ZCode In-app Browser（先例）
  open_questions:
    - question: 浏览器宿主与视口
      ruling: 未向用户提问；沿用文档先例（docs/visual-test-report.md：ZCode In-app Browser，1280×720 桌面，Planner）作为保守默认，用户可随时否决
      basis: E1 先例 + 用户指令未指定
    - question: 数据基线
      ruling: 未向用户提问；沿用本地 compose 库既有数据做浏览器流程；导入/转换新功能用 datasets/ 样例与模板实测，不依赖生产数据
      basis: E1 先例 + 昨日报告同库验证
    - question: NFR 指标（I 类扫描命中：无性能/兼容性数值）
      ruling: README 已显式将性能基准与移动端列为"尚未完成"（本版不验证）→ 进 scope 非目标，不阻塞
      basis: E1 README 显式排除，等同用户裁定
  clarification_records: 无阻塞级澄清项（九类漏网模式过筛：A–H 未命中本测试需求；I 类已由文档显式排除覆盖）
```

## 给下游的一句话索引

模型路径：`/Users/a1234/Documents/class schedule/需求模型.md`；未裁决阻塞项 0（3 项按先例默认，用户可否决）；测试重点 = 昨日报告未验证项 + 本分支最新变更（主数据启用恢复、空 PDF、可选 Sheet 导入、数据集转换、无教室占位）。
